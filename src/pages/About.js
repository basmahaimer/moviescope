import React from 'react';

export default function About() {
  return (
    <div style={{maxWidth:800, margin:'24px auto', padding:16}}>
      <h2>À propos</h2>
      <p>MovieScope est une application de démonstration conçue pour pratiquer React, le routage et le styling (CSS, CSS Modules, styled-components).</p>
    </div>
  );
}
